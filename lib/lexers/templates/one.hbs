      {{#if @first}}
      function () {
      {{else}}
    , function () {
      {{/if}}
              if (input.charCodeAt(i) == {{charCode}}) {
          currentState = {{targetState}}
          {{#if targetIsAccepted}}
          isMatched = true
          {{else}}
          isMatched = false
          {{/if}}
        }
        else {
{{>fallbackPartial}}
        }
      }
